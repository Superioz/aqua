version: "3.1"

services:
  aqua:
    image: superioz/aqua:0.4.0
    build: .
    env_file:
    - .env
    ports:
    - 8765:8765
    restart: always
    volumes:
    - ${PWD}/auth.yml:/etc/aqua/auth.yml
    - aqua:/var/lib/aqua

volumes:
  aqua: